## 2026-03-11 — Fix project tabs overflow in navbar

- Project tabs container now scrolls horizontally when there are many tabs (hidden scrollbar)
- Nav brand, nav links, and nav user area are fixed-size (`flex-shrink: 0`) so tabs don't push them around
- Nav main section takes remaining space with `flex: 1; min-width: 0; overflow: hidden`
