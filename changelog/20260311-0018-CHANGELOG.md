## 2026-03-11 — Fix CSP violation for project base path

- Removed inline script that set `projectBasePath` (blocked by CSP)
- Pass base path via data attribute on the script tag instead
- JS reads `data-base-path` from the script element on load
