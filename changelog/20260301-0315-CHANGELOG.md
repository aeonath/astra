# Changelog — 2026-03-01 03:15

## Fix false "email already in use" error when editing users

Replaced the try/catch UNIQUE constraint approach with an explicit check that only rejects the email if it belongs to a *different* user. Editing a user without changing their email no longer triggers a false duplicate error.
