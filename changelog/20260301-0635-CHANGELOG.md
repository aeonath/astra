# Changelog — 2026-03-01 06:35

## Added: Project Homepage & GitHub URL Fields

- Added `homepage_url`, `github_url`, and `github_private` columns to projects (migration 010)
- Admin project form now includes Homepage URL and GitHub URL fields, plus a "GitHub repo is private" checkbox
- Click-to-edit on admin projects table handles the new URL fields
- Project detail page displays Homepage and GitHub links below the project description
- Visibility rules: Homepage URL is always shown if set; GitHub URL respects the private flag (only shown to logged-in users when marked private)
- Added CSS for project links with arrow indicator
