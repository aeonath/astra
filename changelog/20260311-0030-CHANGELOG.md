## 2026-03-11 — Remove package-lock.json from version control

- Added `package-lock.json` to `.gitignore`
- Each environment generates its own lockfile from `npm install`
- Fixes merge conflict on production deploys
