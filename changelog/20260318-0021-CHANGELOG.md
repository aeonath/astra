# Changelog — 2026-03-18

## Fixed
- All timestamps now use system local time instead of UTC. Changed all `datetime('now')` calls to `datetime('now', 'localtime')` across routes, reset-password script, and migration runner.
- Migration 029 converts all existing UTC timestamps in the database (projects, bugs, users, comments, submissions) to local time.
