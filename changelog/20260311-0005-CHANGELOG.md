## 2026-03-11 — Redesign admin users page

- Removed the Actions column from the users table
- Moved "Manage Submissions" toggle into the Add/Edit User form panel
- Moved "Remove User" button into the form panel (only visible when editing a non-admin user)
- Submissions permission is now saved with the Create/Save action instead of a separate toggle
- Delete confirmation modal is triggered from the form panel's Remove button
- Removed the standalone toggle-submissions route (no longer needed)
