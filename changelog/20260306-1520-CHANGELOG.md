# Changelog — 2026-03-06 15:20

## Fix advanced search filter layout — compact side-by-side selects

- Replaced broken `.advanced-search-form .search-row` grid (selector never matched — form uses id, not class)
- Changed to `.search-row` flex layout with wrap, so filter selects sit compactly side by side
- `.search-row-main` override keeps the main search bar row center-aligned
- `.search-text-group` gets `flex: 1` so the text input expands to fill available space
- `#advanced-panel .form-group` gets `flex: 0 0 auto; min-width: 150px` so selects are compact, not full-width
