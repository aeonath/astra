# Changelog — 2026-03-06 15:18

## Rework advanced search — hidden by default, toggled by link

- Advanced filter panel is hidden by default; revealed via "Advanced ▾" toggle link next to the Search button
- Empty search shows "No results. Try Advanced Search" with a link that opens the panel
- Results page shows "Adjust filters" link that also opens the panel
- Panel auto-opens when advanced filters are active (navigated back with filter params)
- search-toggle.js: CSP-safe external script reads data-advanced-open attribute to control panel state
- Route: added hasAdvancedFilter (non-q filter params) separate from hasAnyFilter
