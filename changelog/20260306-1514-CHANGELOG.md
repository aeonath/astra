# Changelog — 2026-03-06 15:14

## Public project view always shows open bugs and FRs — removed toggle

- Removed public_project_access guard from GET /projects/:slug — project pages are always accessible to public users for public projects
- Public users are always forced to showStatus='open' regardless of ?show= query param
- Open/closed switcher tabs (count-tab) hidden for public users in feature requests and bugs sections
- Todos and notes sections were already gated behind currentUser — no change needed
- Removed public_project_access checkbox from admin site settings view
- Removed public_project_access from admin POST /settings handler
