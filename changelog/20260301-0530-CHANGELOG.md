# Changelog — 2026-03-01 05:30

## Fix public view issues: todos, categories, login, logo

- **Hidden todo stats from public view:** The landing page was showing "Open Todos" counts to non-logged-in visitors. Wrapped in `currentUser` check so only logged-in users see todo stats (on both landing page and projects index).
- **Added category headings to landing page:** The landing page now groups projects by category with headings (matching the /projects page), pulling categories ordered by `sort_order`. Uncategorized projects appear under "Other".
- **Login button moved to navbar:** The navbar now shows for all visitors. Non-logged-in users see the brand, Projects link, and a "Log In" button in the upper right. Removed the login button from the landing page body.
- **Astra logo on login page links to home:** Clicking the star/Astra title on the login page now navigates back to the main home page.
- **Nav brand links to home:** The Astra brand in the navbar now links to `/` (home) instead of `/projects`.
