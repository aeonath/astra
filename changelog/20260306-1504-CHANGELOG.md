# Changelog — 2026-03-06 15:04

## Disable Add Comment on closed items

- CSS: grays out the comment textarea and submit button (opacity 0.35, pointer-events none) when .bug-is-closed is active
- Server: POST /bugs/:id/comment now checks bug status and rejects with an error flash if the item is closed
