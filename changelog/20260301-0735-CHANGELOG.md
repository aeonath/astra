# Changelog — 2026-03-01 07:35

## Added: Database backup script

- New `astra_db_backup.sh` script that reads DB_PATH from `.env`, creates a safe SQLite backup, and gzip-compresses it with a date stamp (e.g. `astra.db-20260301.gz`)
- Added "Database Backups" section to SETUP.md with manual and crontab instructions
- Added `sqlite3` CLI to server dependency install instructions (required by the backup script)
