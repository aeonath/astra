# Changelog — 2026-03-06 12:20

## Fix bug/feature/todo detail view layout

- Removed the `--` prefix that was incorrectly prepended to the description/title on the bug detail page
- Moved the description (title) display inside the same gray card as Additional Notes, so both are visually grouped together
- Added a subtle separator line between the title and Additional Notes within the card
- Cleaned up stray `<br/>` tags that were used for spacing
