# Changelog — 2026-03-01 03:30

## Update README.md with current database workflow and admin password instructions

Updated the README to reflect the new database initialization workflow (`db:init`, `db:migrate`), document that `DB_PATH` must be an absolute path, and note that admin passwords can only be changed via the CLI (`npm run reset-password`). Added a full scripts reference table and database section.
