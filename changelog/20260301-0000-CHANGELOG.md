# Changelog — 2026-03-01 00:00

## Add TODO lists and simplify statuses

- Added `todo` as a third item type alongside bugs and feature requests
- Simplified statuses to just **open** and **closed** (removed in_progress, resolved, wontfix)
- Migration recreates the bugs table with updated CHECK constraints and maps old statuses to closed
- Project detail page now shows all three lists simultaneously (todos on top, features middle, bugs bottom) instead of tabbed view
- Each section has a colored left border accent (teal for todos, purple for features, red for bugs)
- Three action buttons for logged-in users: "Add Todo" (teal), "Request Feature" (purple), "Report Bug" (blue)
- "Show All / Show Open Only" toggle replaces the old status filter tabs
- Landing page simplified to show three open counts per project (bugs, features, todos)
- Project index cards show open counts for all three types
- Bug detail sidebar status dropdown reduced to open/closed
- Added teal color theme (`--teal: #39d2c0`) for todo buttons, badges, and stats
- Admin dashboard query updated for simplified status
- Seed script creates projects as public by default
