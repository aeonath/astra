## 2026-03-11 — Fix manage submissions permission check

- Changed `can_manage_submissions === 1` to a truthy check (`!!user.can_manage_submissions`)
- The strict equality check could fail if the value wasn't exactly the integer `1`
- This fixes the Submissions link not appearing in the user dropdown for users with the permission enabled
