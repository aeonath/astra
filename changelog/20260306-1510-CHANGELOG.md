# Changelog — 2026-03-06 15:10

## Redirect to project table view after closing a bug/FR/todo

- POST /bugs/:id now detects when status transitions to closed (and wasn't already closed)
- On close, redirects to /projects/:slug instead of back to the bug detail page
- If already closed or status unchanged, redirects as before to the bug detail page
