# Changelog — 2026-02-28 22:15

## Fix stale server bugs and harden migrations

- Both reported bugs (landing page redirect, `viewType is not defined`) were caused by a stale server process running old code — the current codebase was already correct
- Made the seed Astra project **public** by default (`public = 1`) so visitors can see it on the landing page
- Updated the existing database record to set Astra project as public
- Hardened the migration system with try-catch to handle fresh databases where columns already exist in the CREATE TABLE (prevents "duplicate column" crash)
