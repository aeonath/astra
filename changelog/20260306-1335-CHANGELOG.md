# Changelog — 2026-03-06 13:35

## Show app version in footer for admins

- Injected appVersion from package.json into res.locals via the locals middleware
- Footer displays version (e.g. "v1.0.0") at the far right, visible only when logged in as admin
- Non-admin and logged-out users see no version text (blank fallback)
