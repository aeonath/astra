# Changelog — 2026-03-18

## Fixed
- All INSERT statements now explicitly set timestamps with `datetime('now', 'localtime')` instead of relying on table DEFAULT values (which were still UTC). This affects bugs, comments, projects, users, categories, public submissions, and the seed script.
