## 2026-03-11 — Fix manage submissions toggle not persisting

- Changed toggle-submissions route to use an explicit value from the form instead of flipping the current DB value
- Added a hidden input with the desired value so the server doesn't need to read-then-toggle
- Prevents race conditions or double-submit issues that could cause the value to flip back
