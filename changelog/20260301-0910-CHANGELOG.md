# Changelog — 2026-03-01 09:10

## Added: "In Progress" status for bugs, features, and todos

- New `in_progress` status option alongside `open` and `closed` (migration 015)
- Status dropdown on bug/feature/todo detail page now includes "in progress"
- Items with `in_progress` status count as "open" in all summary counts
- Project detail page shows `in_progress` items in the open tab
- Badge styling for `in_progress` uses blue/accent color (already existed in CSS)
- All count queries updated to use `status != 'closed'` instead of `status = 'open'`
