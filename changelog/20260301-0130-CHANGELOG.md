# Changelog — 2026-03-01 01:30

## Add CLI password reset tool

Added `npm run reset-password` command to reset any user's password from the command line without needing the web UI.

Usage: `npm run reset-password -- <username> <new-password>`
