# Changelog — 2026-03-01 01:45

## Fix SETUP.md to use `sudo su - astra` pattern

All instructions now switch to the `astra` user first with `sudo su - astra` instead of using `cd /home/astra/app && sudo -u astra ...`, which fails when the admin user can't access `/home/astra`. Also added the `reset-password` CLI usage to the setup instructions.
